version: '3.8'

services:
  affiliate-app:
    image: python:3.11-slim
    container_name: affiliate_website_fixed
    working_dir: /app
    ports:
      - "80:8000"
    environment:
      # Database Configuration - Supabase
      - DATABASE_HOST=db.yyksfmfrsiewpiwajtzw.supabase.co
      - DATABASE_PORT=5432
      - DATABASE_NAME=postgres
      - DATABASE_USER=postgres
      - DATABASE_PASSWORD=Ranjith123
      
      # Supabase Configuration
      - NEXT_PUBLIC_SUPABASE_URL=https://yyksfmfrsiewpiwajtzw.supabase.co
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inl5a3NmbWZyc2lld3Bpd2FqdHp3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTM1MjYzMjMsImV4cCI6MjA2OTEwMjMyM30.qXUi52X7HNChoCyiroSX5Nh48PZlNfVWhzkNquHh130
      
      # Cuelinks API Configuration
      - CUELINKS_API_KEY=MUmQPF2MLjDzMOHi0PSCdOwI082JAfj6vRLLT1QcY00
      - CUELINKS_BASE_URL=https://www.cuelinks.com/api/v2
      
      # Application Configuration
      - SECRET_KEY=supersecret-affiliate-oauth-key-2025-production-ready
      - DEBUG=False
      
      # Google OAuth Configuration
      - GOOGLE_CLIENT_ID=1073044995927-280c34duigls0dfm2j05racte5rebfoa.apps.googleusercontent.com
      - GOOGLE_CLIENT_SECRET=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
      - GOOGLE_REDIRECT_URI=http://31.97.237.145/auth/callback
      
    command: |
      bash -c "
      apt-get update && apt-get install -y git gcc &&
      rm -rf /app/* /tmp/repo* &&
      REPO_DIR=/tmp/repo-$(date +%s) &&
      git clone https://github.com/ranjith888999/AffilaiteWebsite.git \$REPO_DIR &&
      cp -r \$REPO_DIR/* /app/ &&
      cd /app &&
      git checkout feature/superbase_url 2>/dev/null || echo 'Already on correct branch' &&
      pip install fastapi uvicorn gunicorn sqlalchemy psycopg2-binary python-multipart jinja2 python-dotenv requests &&
      mkdir -p /var/log/affiliate-website &&
      echo 'Starting FastAPI application...' &&
      python -m uvicorn main:app --host 0.0.0.0 --port 8000
      "
    restart: unless-stopped

networks:
  default:
    name: affiliate_network
